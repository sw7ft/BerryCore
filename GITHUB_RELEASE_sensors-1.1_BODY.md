## sensors 1.1 — battery and CPU

Same motion CLI as 1.0, plus system sensors:

```
qpkg install sensors
sensors battery
sensors cpu
sensors sys
```

Bare `sensors` still one-shots accel/gyro/mag/… then prints battery + cpu.

Zip: `util-sensors-1.1.zip`. Alias: `sensor`.
