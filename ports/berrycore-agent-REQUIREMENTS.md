# berrycore-agent — Requirements

**Install python3 first:**

```bash
qpkg install python3
```

Then install berrycore-agent:

```bash
qpkg install berrycore-agent
```

The agent uses Python 3.11 stdlib only (no pip dependencies). It will not work without
the python3 port installed.
